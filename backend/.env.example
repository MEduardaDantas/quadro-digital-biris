PORT=3000
DATABASE_URL=postgres://postgres:postgres@localhost:5432/quadro_digital
UPLOADS_DIR=./uploads
SESSION_TTL_DAYS=30
CORS_ORIGIN=http://localhost:8080
MAX_UPLOAD_MB=10
# NODE_ENV=production   # descomente em produção — trava o "npm run seed"
                         # (que cria demo-frame/1234) pra não rodar sem querer
